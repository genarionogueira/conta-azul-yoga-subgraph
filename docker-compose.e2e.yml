services:
  mock-conta-azul:
    image: wiremock/wiremock:3.9.1
    volumes:
      - ./wiremock/mappings:/home/wiremock/mappings:ro
    healthcheck:
      test: ['CMD', 'curl', '-f', 'http://localhost:8080/__admin/health']
      interval: 5s
      retries: 10
      start_period: 10s

  mongo:
    ports: !override
      - '0:27017'

  redis:
    ports: !override
      - '0:6379'

  yoga-subgraph:
    ports: !override
      - '0:4000'
    depends_on:
      redis:
        condition: service_healthy
      mongo:
        condition: service_healthy
      mock-conta-azul:
        condition: service_healthy
    environment:
      CONTA_AZUL_API_BASE_URL: http://mock-conta-azul:8080
      CONTA_AZUL_CLIENT_ID: e2e-client-id
      CONTA_AZUL_CLIENT_SECRET: e2e-client-secret
      CONTA_AZUL_TOKEN_URL: http://mock-conta-azul:8080/oauth2/token
      CONTA_AZUL_REDIRECT_URI: http://mock-conta-azul:8080/callback
      CONTA_AZUL_AUTH_URL: http://mock-conta-azul:8080/oauth2/authorize
      MONGODB_URL: mongodb://mongo:27017/conta_azul
    healthcheck:
      interval: 5s
      timeout: 3s
      retries: 10
      start_period: 15s
